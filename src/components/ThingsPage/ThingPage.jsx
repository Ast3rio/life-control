import React from 'react';
import styles from './ThingsPage.module.scss';

export const ThingPage = () => {
    return (
        <section className={styles.things}>
            Things
        </section>
    );
};
